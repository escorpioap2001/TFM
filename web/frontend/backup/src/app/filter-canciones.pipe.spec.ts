import { FilterCancionesPipe } from './filter-canciones.pipe';

describe('FilterCancionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCancionesPipe();
    expect(pipe).toBeTruthy();
  });
});
